# WI-R4 — Global Audit Assurance Overlay

**Node:** R4  
**Base artifact:** `docs/remediation-contracts/WI-R4.md` at `3839e856986b7034d55b12be7f59f000b4a97123`  
**Overlay status:** OPEN  
**Implementation eligibility:** SUSPENDED  
**Local transcription/source history preserved:** YES  
**Global promotion eligibility:** NOT YET

## 1. Purpose

This overlay prevents R4 from appearing globally cleaner than the Global Fidelity Audit currently supports.

The base artifact remains immutable evidence of what was reviewed. This overlay records the Phase A provenance limitations discovered after the earlier R4 verification record was written.

## 2. Open global-audit items

### GA-R4-01 — R4-A1 source provenance uncertainty

**Status:** OPEN / SOURCE EXHAUSTED FROM CURRENT REVIEWER SOURCE BASIS

The exact historical provenance of the R4-A1 audit structure, classification vocabulary, and `R4-M9+` numbering convention cannot be distinguished from later cross-node audit conventions using the currently available same-continuity reviewer memory.

This does not prove the R4-A1 content is wrong. It means the exact R4-specific historical source claim is not currently T3-proven.

**Implementation effect:** implementation authority remains suspended pending the Global Audit. Any eventual implementation that relies on the exact R4-A1 labels/numbering as historically frozen must either obtain source support or be explicitly adopted as a conservative corrected-governance rule by the final authority set.

### GA-R4-02 — exact 27-item closure-list form provenance

**Status:** OPEN / SOURCE EXHAUSTED FROM CURRENT REVIEWER SOURCE BASIS

The substantive obligations are largely grounded elsewhere in R4, but the exact 27-item count/order/form cannot be independently distinguished from reconstruction synthesis.

**Implementation effect:** the checklist may remain a conservative closure aid, but its exact historical form must not be represented as source-fidelity proven without new evidence.

### GA-R4-03 — Human Action three-state versus general four-state vocabulary

**Status:** OPEN / TARGETED T4 SCRUTINY

Current interpretation: the three-state Human Action creation model is contextual and intentionally excludes `STALE_LINEAGE`, while the general four-state vocabulary includes stale eligibility for objects whose exact lineage is known.

No contradiction is presently demonstrated. Independent review should confirm that this is genuinely source-consistent rather than post-hoc rationalization.

## 3. Assurance effect

R4 remains below T3 from the currently available Phase A source basis.

The prior R4 verification history remains valid for the narrower claim it actually established: committed-artifact fidelity to the reconstruction used in that earlier review. It must not be relabeled as direct source-fidelity verification.

No open item in this overlay is permission to invent replacement content.

## 4. Closure rule

This overlay may close only when:

- GA-R4-01 and GA-R4-02 are either source-resolved, independently re-derived, or explicitly adjudicated as corrected-governance rules rather than historical-source claims;
- GA-R4-03 receives independent scrutiny;
- any resulting R4 artifact amendment receives a new immutable SHA;
- every dependent edge/compound/hard-chain certification against the superseded R4 SHA is invalidated and rerun;
- the final assurance matrix reflects the actual resulting tier.

## 5. References

- Phase A review: `docs/remediation-contracts/WI-R4-PHASE-A-SOURCE-REVIEW.md`
- Governing audit: `docs/remediation-contracts/GLOBAL_FIDELITY_CROSS_NODE_AUDIT.md`
- Earlier verification record: `docs/remediation-contracts/WI-R4-FIDELITY-VERIFICATION.md`

## 6. Relay-contamination guard

This overlay terminates here. No conversational handoff text is part of the governing overlay.